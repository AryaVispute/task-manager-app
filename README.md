# FlowDo - Task Manager Application

# Description
A clean and minimal full-stack task manager built using React and Supabase.

# Features & Speciality
- Add, edit, delete tasks
- Mark tasks as completed
- Filter tasks (all / completed / pending)
- Persistent data using Supabase
- Clean and responsive UI

# Tech Stack
- React (Vite)
- Tailwind CSS
- Supabase

# How to Run
1. Clone repository
2. Install dependencies:
   npm install
3. Add environment variables in .env:
   VITE_SUPABASE_URL=your_url
   VITE_SUPABASE_ANON_KEY=your_key
4. Run project:
   npm run dev

# Note
"We value clarity, correctness, and sensible trade-offs."
